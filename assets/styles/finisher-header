.hero {
    position: relative;
    height: 27vh;
    overflow: hidden;
    color: white;
}

.finisher-header {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    z-index: -1;
}

.hero h1, 
.hero p {
    z-index: 1;
    position: relative;
    color: white; /* Ensures text is visible over the background */
}
